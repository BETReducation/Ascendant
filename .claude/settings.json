{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd /Users/garybyatt/Documents/GitHub/Ascendant && git add -A && git diff --cached --quiet || git commit -m \"Auto-save changes\" && git push origin main 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Saving to GitHub..."
          }
        ]
      }
    ]
  }
}
